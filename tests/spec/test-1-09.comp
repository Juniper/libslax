+/* Machine Crafted with Care (tm) by slaxWriter */
 version 1.0;
 
 match * {
     mode "one";
+    
     <one> .;
 }
 
 match * {
     mode "two";
+    
     <two> string-length(.);
 }
 
@@ -18,3 +21,4 @@
 	mode "two";
     }
 }
+
