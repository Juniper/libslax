version 1.2;

main {
    var $configuration = configuration;
    var $a = $configuration/descendant-or-self::*/*[@junos:changed];
    var $b = descendant-or-self::node()/*[@junos:changed];
    var $c = $configuration/descendant-or-self::node()/*[@junos:changed];
}
