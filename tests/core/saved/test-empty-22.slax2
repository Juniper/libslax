/* Machine Crafted with Care (tm) by slaxWriter */
version 1.1;

match / {
    <top> {
        var $x = /test;
        
        if (not($x) && not(/foo/bar) && not(goo[zoo && not(moo)])) {
            <not>;
        }
    }
}
