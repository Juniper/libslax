/* Machine Crafted with Care (tm) by slaxWriter */
version 1.0;

template test () {
    expr "SUCCESS";
}

match / {
    var $foo = {
        call test();
    }
    
    if (1) {
        expr $foo;
    }
}
